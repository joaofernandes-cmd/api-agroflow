import React from "react";
import ReactDOM from "react-dom/client";
import CapatazApp from "./CapatazApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CapatazApp />
  </React.StrictMode>
);
